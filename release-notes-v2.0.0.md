Today Trajectory 2.0.0 桌面版本。

本次发行包含：

- 点击关闭按钮时，询问是退出软件还是最小化到后台
- 增加系统托盘入口，可重新打开主窗口或直接退出
- 倒计时归零后，桌宠自动完成收尾并自动隐藏
- 四段式清脆结束铃声提醒
- 记录页支持删除单条记录
- 本地 `records.json` 文件持久化存储

安装说明：

- 下载附件中的 Windows 安装包
- 双击安装后即可使用
- 如果点击关闭后选择最小化到后台，可从系统托盘重新打开
- 目标系统需已安装 WebView2 Runtime
